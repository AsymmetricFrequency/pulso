# Paquetes compartidos

- `schemas`: contratos Zod usados en los límites de red.
- `domain`: interfaces y errores del dominio, independientes de infraestructura.

Los siguientes paquetes previstos son `database`, `authorization`, `sync-engine`, `geospatial` y `ui`.
